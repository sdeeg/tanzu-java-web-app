---
swagger: "2.0"
info:
  description: "Swagger API definition for the greeting"
  version: "0.0.2"
  title: "APIs to interact with greeters"
host: greeting.planet10.lab
basePath: "/v1"
tags:
- name: "Greeter"
  description: "Greet API"
schemes:
- "http"
paths:
  /hello:
    get:
      tags:
      - "Data collection"
      summary: "Get hello"
      produces:
      - "application/json"
      responses:
        200:
          description: "successful greeting"
        400:
          description: "no greeting for you"